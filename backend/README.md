# TalentFlow Backend (Ktor)

Simple Ktor server exposing /ping and /api/health.

## Run locally
- With Gradle wrapper (add wrapper first if missing): `./gradlew run`

## Docker
Built by root docker-compose.
