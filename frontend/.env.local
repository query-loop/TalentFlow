# Point directly to the backend when running locally without Docker.
# Leave empty to use Vite proxy (recommended) or set to your backend URL.
# For dev with scripts/dev-up.sh, the backend runs on :8080 and Vite proxies /api,
# so keep this empty to avoid cross-origin/port mismatches causing Failed to fetch.
PUBLIC_API_BASE=
