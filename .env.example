# Example environment (copy to .env)

# Backend database URL:
# - Local sqlite example (default):
# DATABASE_URL=sqlite:////workspaces/TalentFlow/server/.run/talentflow.sqlite
# - Turso/libsql example (host only; token set below):
# DATABASE_URL=libsql://talentflow-primary-pos-tpo.aws-ap-south-1.turso.io

# Turso/libsql auth token (required when using libsql)
# LIBSQL_AUTH_TOKEN=your_turso_token_here

# Frontend/Backend base (optional)
# BACKEND_BASE=http://localhost:8080
# PUBLIC_API_BASE=http://localhost:8080
